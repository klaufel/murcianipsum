import React from 'react'

const baseClass = 'Header'

const Header = () => (
  <header className={baseClass}>
    <h1 className={`${baseClass}-title`}>Murcianipsum 🍋</h1>
  </header>
)

export default Header
